📄 (basename ./all.txt)
────────────────────────────────
────────────────────────────────
📁 (basename ./components)/
  📄 (basename ./components/BlogGrid.astro)
────────────────────────────────
---
// BlogGrid.astro
const { posts } = Astro.props;
---

<section class="blog-grid">
  {posts.map((post) => (
    <a href={post.url} class="blog-card">
      {post.thumbnail && (
        <img src={post.thumbnail} alt={post.title} class="blog-thumb" />
      )}
      <div class="blog-content">
        <h2 class="blog-title">{post.title}</h2>
        <p class="blog-date">{new Date(post.date).toDateString()}</p>
      </div>
    </a>
  ))}
</section>────────────────────────────────
  📄 (basename ./components/BlogLayout.astro)
────────────────────────────────
---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const { title, date, thumbnail } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <link rel="stylesheet" href="/main.css">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🎵</text></svg>">
    <meta charset="Unicode">
    <title>{title} — STEM Project 2025</title>
  </head>
  <body>
    <Header />
    <main class="mainContent post-layout">
      <article>
        <h1>{title}</h1>
        <p class="blog-date">{new Date(date).toDateString()}</p>
        {thumbnail && <img src={thumbnail} alt={title} class="blog-thumb" />}
        <slot />
      </article>
    </main>
    <Footer />
  </body>
</html>
────────────────────────────────
  📄 (basename ./components/Button.astro)
────────────────────────────────
---
// src/components/Button.astro
export interface Props {
    label: string;
    for: string;
    class?: string;
}

const { label, for: forId, class: className } = Astro.props;
---

<label for={forId} class:list={["button", className]}>{label}</label>

<style>
    .button {
        display: inline-block;
        padding: 1rem 2rem;
        border: 2px solid var(--accent-color);
        border-radius: 8px;
        background-color: var(--primary-color);
        color: var(--accent-color);
        font-family: 'Space Grotesk', sans-serif;
        font-size: 1.2rem;
        font-weight: 500;
        text-align: center;
        cursor: pointer;
        transition: all 0.2s ease-in-out;
    }

    .button:hover {
        background-color: var(--accent-color);
        color: var(--primary-color);
    }
</style>
────────────────────────────────
  📄 (basename ./components/Footer.astro)
────────────────────────────────
---
---
<footer class="sub doCenter">
    <hr>
    <span class="centerText">Site fully designed, made & written by Rishi S. Malnad (hamb1y - GitHub)!</span>
</footer>
────────────────────────────────
  📄 (basename ./components/Header.astro)
────────────────────────────────
---
import ThemeSwitcher from "./ThemeSwitcher.astro"
---
<header class="doCenter">
    <hr class="">
    <img src="images/jnvckm-logo.webp" sub="jnvckm-logo" height="80px">
    <ul class="navbar">
        <li><a href="/">Home</a></li>
        <li><a href="/about">About Us</a></li>
        <li><a href="/blogs">Blogs</a></li>
        <li><a href="/events">Events</a></li>
        <li><a href="/contributions">Contributions</a></li>
        <li><a href="/gallery">Gallery</a></li>
        <li><a href="/faq">FAQ</a></li>
        <li><a href="/alumni">Alumni Corner</a></li>
        <li><a href="/students">Student's Corner</a></li>
    </ul>
    
    <ThemeSwitcher />
    <hr class="">
</header>

<style>
    header, footer {
        display: flex;
        justify-content: flex-end; /* Aligns items to the right */
        align-items: center; /* Vertically centers items if they have different heights */
        /* You might want to add some padding or gap here */
        padding: 1rem;
        gap: 1rem; /* Adds space between flex items */
    }

    /* Adjust hr to fit within the flex layout if needed */
    hr {
        width: auto; /* Allow hr to shrink/grow as needed */
        flex-grow: 1; /* Allow hr to take up available space */
        border: none; /* Remove default hr border */
        border-top: 1px solid var(--accent-color); /* Apply your adaptive gray */
    }
</style>
────────────────────────────────
  📄 (basename ./components/Layout.astro)
────────────────────────────────
---
import Header from "./Header.astro";
import Footer from "./Footer.astro";
---
<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="/main.css">
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🎵</text></svg>">
        <meta charset="Unicode">
        <title>STEM Project 2025 - Psychology of Sound 🧠🎵</title>
        
    </head>
    <body>
        <Header />
        <div class="mainContent">
            <slot />
        </div>
        <Footer />
    </body>
</html>────────────────────────────────
  📄 (basename ./components/ThemeSwitcher.astro)
────────────────────────────────
---
import Button from './Button.astro';
---

<div class="themeSwitcher">
    <input type="radio" id="light" name="theme" value="light">
    <Button label="Light" for="light" class="invertLight"/>
    <input type="radio" id="dark" name="theme" value="dark" checked>
    <Button label="Dark" for="dark" class="invertDark"/>
</div>

<style>
    .themeSwitcher input[type="radio"] {
        position: absolute;
        opacity: 0;
        width: 0;
        height: 0;
    }

</style>────────────────────────────────
📁 (basename ./pages)/
  📄 (basename ./pages/about.astro)
────────────────────────────────
────────────────────────────────
  📄 (basename ./pages/all.txt)
────────────────────────────────
📄 (basename ./about.astro)
────────────────────────────────
────────────────────────────────
📄 (basename ./all.txt)
────────────────────────────────
────────────────────────────────
📄 (basename ./alumni.astro)
────────────────────────────────
────────────────────────────────
📁 (basename ./blogs)/
📄 (basename ./blogs.astro)
────────────────────────────────
---
import Layout from '../components/Layout.astro';
import BlogGrid from '../components/BlogGrid.astro';

const postImports = import.meta.glob('./blogs/*.md', { eager: true });

const posts = Object.entries(postImports).map(([path, post]) => ({
  url: path.replace('./blogs', '/blogs').replace('.md', ''),
  title: post.frontmatter.title,
  date: post.frontmatter.date,
  thumbnail: post.frontmatter.thumbnail,
  content: post.compiledContent(),
}));
---

<Layout title="All Blog Posts">
  <h1><ul><b>All Blog Posts:</b></ul></h1>
  <BlogGrid posts={posts} />
</Layout>────────────────────────────────
📄 (basename ./contributions.astro)
────────────────────────────────
---
import Layout from '../components/Layout.astro';

const postImports = import.meta.glob('./contributions/*.md', { eager: true });

const posts = Object.entries(postImports).map(([path, post]) => ({
  url: path.replace('./blogs', '/blogs').replace('.md', ''),
  title: post.frontmatter.title,
  date: post.frontmatter.date,
  thumbnail: post.frontmatter.thumbnail,
  content: post.compiledContent(),
}));
---

<Layout title="All Contributions">
  <h1><ul><b>All Contributions:</b></ul></h1>
  {posts.map(post => (
    <article>
      <h2><a href={post.url}>{post.title}</a></h2>
      <p><em>{new Date(post.date).toDateString()}</em></p>
      {post.thumbnail && <img src={post.thumbnail} alt={post.title} style="max-width: 200px;" />}
    </article>
  ))}
</Layout>────────────────────────────────
📄 (basename ./events.astro)
────────────────────────────────
---
import Layout from '../components/Layout.astro';

const postImports = import.meta.glob('./events/*.md', { eager: true });

const posts = Object.entries(postImports).map(([path, post]) => ({
  url: path.replace('./blogs', '/blogs').replace('.md', ''),
  title: post.frontmatter.title,
  date: post.frontmatter.date,
  thumbnail: post.frontmatter.thumbnail,
  content: post.compiledContent(),
}));
---

<Layout title="All Events">
  <h1><ul><b>All Events:</b></ul></h1>

  {posts.map(post => (
    <article>
      <h2><a href={post.url}>{post.title}</a></h2>
      <p><em>{new Date(post.date).toDateString()}</em></p>
      {post.thumbnail && <img src={post.thumbnail} alt={post.title} style="max-width: 200px;" />}
    </article>
  ))}
</Layout>────────────────────────────────
📄 (basename ./faq.astro)
────────────────────────────────
────────────────────────────────
📄 (basename ./gallery.astro)
────────────────────────────────
────────────────────────────────
📄 (basename ./index.astro)
────────────────────────────────
---
import Button from "../components/Button.astro";
import ThemeSwitcher from "../components/ThemeSwitcher.astro";
import Layout from "../components/Layout.astro";
---

<Layout>
	<h1>Hello!</h1>
</Layout>────────────────────────────────
📄 (basename ./students.astro)
────────────────────────────────
────────────────────────────────
────────────────────────────────
  📄 (basename ./pages/alumni.astro)
────────────────────────────────
────────────────────────────────
  📁 (basename ./pages/blogs)/
  📄 (basename ./pages/blogs.astro)
────────────────────────────────
---
import Layout from '../components/Layout.astro';
import BlogGrid from '../components/BlogGrid.astro';

const postImports = import.meta.glob('./blogs/*.md', { eager: true });

const posts = Object.entries(postImports).map(([path, post]) => ({
  url: path.replace('./blogs', '/blogs').replace('.md', ''),
  title: post.frontmatter.title,
  date: post.frontmatter.date,
  thumbnail: post.frontmatter.thumbnail,
  content: post.compiledContent(),
}));
---

<Layout title="All Blog Posts">
  <h1><ul><b>All Blog Posts:</b></ul></h1>
  <BlogGrid posts={posts} />
</Layout>────────────────────────────────
  📄 (basename ./pages/contributions.astro)
────────────────────────────────
---
import Layout from '../components/Layout.astro';

const postImports = import.meta.glob('./contributions/*.md', { eager: true });

const posts = Object.entries(postImports).map(([path, post]) => ({
  url: path.replace('./blogs', '/blogs').replace('.md', ''),
  title: post.frontmatter.title,
  date: post.frontmatter.date,
  thumbnail: post.frontmatter.thumbnail,
  content: post.compiledContent(),
}));
---

<Layout title="All Contributions">
  <h1><ul><b>All Contributions:</b></ul></h1>
  {posts.map(post => (
    <article>
      <h2><a href={post.url}>{post.title}</a></h2>
      <p><em>{new Date(post.date).toDateString()}</em></p>
      {post.thumbnail && <img src={post.thumbnail} alt={post.title} style="max-width: 200px;" />}
    </article>
  ))}
</Layout>────────────────────────────────
  📄 (basename ./pages/events.astro)
────────────────────────────────
---
import Layout from '../components/Layout.astro';

const postImports = import.meta.glob('./events/*.md', { eager: true });

const posts = Object.entries(postImports).map(([path, post]) => ({
  url: path.replace('./blogs', '/blogs').replace('.md', ''),
  title: post.frontmatter.title,
  date: post.frontmatter.date,
  thumbnail: post.frontmatter.thumbnail,
  content: post.compiledContent(),
}));
---

<Layout title="All Events">
  <h1><ul><b>All Events:</b></ul></h1>

  {posts.map(post => (
    <article>
      <h2><a href={post.url}>{post.title}</a></h2>
      <p><em>{new Date(post.date).toDateString()}</em></p>
      {post.thumbnail && <img src={post.thumbnail} alt={post.title} style="max-width: 200px;" />}
    </article>
  ))}
</Layout>────────────────────────────────
  📄 (basename ./pages/faq.astro)
────────────────────────────────
────────────────────────────────
  📄 (basename ./pages/gallery.astro)
────────────────────────────────
────────────────────────────────
  📄 (basename ./pages/index.astro)
────────────────────────────────
---
import Button from "../components/Button.astro";
import ThemeSwitcher from "../components/ThemeSwitcher.astro";
import Layout from "../components/Layout.astro";
---

<Layout>
	<h1>Hello!</h1>
</Layout>────────────────────────────────
  📄 (basename ./pages/students.astro)
────────────────────────────────
────────────────────────────────
